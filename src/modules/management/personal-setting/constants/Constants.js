import { config } from '../../../../common/config';

export const NAMESPACE = 'personalSetting';

/**
 * 请求个人中心数据URL
 * @type {string}
 */
export const RESOURCE_URL = `${config.contextPath}/api/users`;
